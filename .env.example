# Teacher/Recording Mode Features
# Set to "1" to enable invisible scroll buttons for tablet recording sessions
# This adds two transparent touch zones on the left side for bidirectional auto-scrolling during recordings
# - UP button: positioned at top 0 to 1/3 of screen (scrolls content upward)
# - DOWN button: positioned at top 1/3 to 2/3 of screen (scrolls content downward)
# The buttons are completely invisible (opacity-0) unless debug mode is enabled in the component
# VITE_TEACHER_SCROLL=1
